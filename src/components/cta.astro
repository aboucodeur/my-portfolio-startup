---
import Link from "./ui/link.astro";
import { getLangFromUrl, useTranslations } from "i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<div
  class="bg-blue-500 p-8 md:px-20 md:py-20 mt-20 mx-auto max-w-5xl rounded-lg flex flex-col items-center text-center">
  <h2 class="text-white text-4xl md:text-6xl tracking-tight">
    {t('common.cta.title')} !
  </h2>
  <p class="text-white mt-5 text-lg md:text-xl">
      Ma mission est avec mes competences avoir un impact sur ma communaute locale, le monde comme le 
      ferait tout bon developpeur.
  </p>
  <div class="flex mt-5">
    <Link href="/contact" class="rounded-full" style="inverted">
      {t('common.cta.button')} !
    </Link>
  </div>
</div>
